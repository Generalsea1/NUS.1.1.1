Status: implementation branch prepared from production `main` after PR #40 merge.

Do not merge this branch until repository dependency/platform configuration is verified and CI proves analyze + tests + APK build.
