# Voice Input implementation

The voice capture UI is review-first: recognition produces text, the user reviews/retries, and only the existing Quick Add/reminder save path persists a reminder. No audio persistence or parallel reminder repository is introduced.
