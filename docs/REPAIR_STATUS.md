# Direct repair status

The startup authentication path was corrected so the app no longer launches Google OAuth automatically on first launch. NUS account authentication is user-driven through the email/password account flow, while AI-provider authorization remains separate.
